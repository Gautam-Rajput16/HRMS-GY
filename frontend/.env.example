# API URL for development (optional - defaults to http://localhost:5000/api in dev mode)
# VITE_API_URL=http://localhost:5000/api

# For production builds, the API URL defaults to the production backend
# If you need to override it, uncomment and set:
# VITE_API_URL=https://hrms-backend-rose.vercel.app/api
